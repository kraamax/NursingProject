import { PersonalBackground } from './personal-background';

describe('PersonalBackground', () => {
  it('should create an instance', () => {
    expect(new PersonalBackground()).toBeTruthy();
  });
});
